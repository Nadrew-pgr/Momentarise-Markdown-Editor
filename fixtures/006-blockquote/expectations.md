# Expectations

- Preserve blockquote markers, blank quoted lines, nested quote depth, and the paragraph after the quote.
- Normalized output may remove trailing spaces but must not flatten nesting.
- Opaque handling is not expected for standard blockquotes.
- Source-only handling is not required unless a rich editor cannot represent nested quote depth.
- Render as a blockquote with a nested blockquote.
