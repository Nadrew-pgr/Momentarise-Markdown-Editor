# Expectations

- Preserve hashtags, wikilinks, task items, sanitized placeholder names, example.invalid contact, and heading structure.
- Normalized output may standardize blank lines but must not remove vault-like linking syntax.
- Opaque handling is acceptable for hashtags and wikilinks until dedicated nodes exist.
- Source-only handling is acceptable in rich mode if vault constructs are not editable structurally.
- Render as a vault-like note with safe placeholder data only.
