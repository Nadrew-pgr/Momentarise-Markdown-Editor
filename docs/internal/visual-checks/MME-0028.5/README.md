# MME-0028.5 Visual Checks

Generated by:

```sh
npm run visual:mme-0028.5
```

Artifacts:

- `inline-ai-prompt-rich.png` — `/ai` in rich mode opens the reusable inline prompt under the current Markdown block, with prompt focus and explicit mock/offline provider state.
- `inline-ai-suggestion-staged.png` — selecting an inline AI action stages generated content with accept/reject controls instead of mutating Markdown silently.
- `inline-ai-provider-missing.png` — missing provider state is explicit and disables prompt submission; the demo does not imply raw OpenAI, Gemini, or Mistral keys work.
- `inline-ai-policy-blocked.png` — policy-denied Markdown blocks AI before provider egress and shows the blocked staged state.
