# MME-0035 Visual Checks

Generated by:

```sh
npm run visual:mme-0035
```

Local URL:

- `http://127.0.0.1:5174/`

Artifacts:

- `source-autosave-clean.png` proves source edits autosave back to `clean` without switching editor modes.
- `external-clean-auto-applied.png` proves a clean local buffer accepts an external file change and remains `clean`.
- `external-dirty-conflict-actions.png` proves a dirty local buffer keeps local edits, detects conflict before overwrite, and shows resolution actions.
- `external-conflict-reloaded.png` proves `Reload external` discards the local conflict and reloads the external version as `clean`.
