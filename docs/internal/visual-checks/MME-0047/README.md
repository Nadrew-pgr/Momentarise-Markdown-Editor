# MME-0047 Visual Checks

This folder stores folding polish artifacts.

- `folding-quiet-gutter-focus.png` proves fold controls are subtle by default and keyboard-focusable with labels.
- `folding-code-block-collapsed.png` proves a code block can fold without mutating Markdown.
- `folding-callout-collapsed.png` proves a callout-like block can fold without mutating Markdown.
- `folding-opaque-block-collapsed.png` proves a preserved opaque/raw block can fold without mutating Markdown.
- `folding-parent-child-state.png` proves nested heading fold state stays predictable through parent collapse and expand.
