# MME-0025 Visual Checks

Generated by:

```sh
npm run visual:mme-0025
```

Expected local URL:

```text
http://127.0.0.1:5174/
```

Artifacts:

- `theme-default-dark-toolbar.png`: default dark token scheme with the default `IconSet` rendered in the rich toolbar.
- `theme-light-toolbar.png`: light scheme applied through `data-mme-scheme="light"` without component CSS changes.
- `theme-host-override.png`: host partial `MmeTheme` applied at runtime through `resolveThemeToCssVariables`, proving accent, radius, density, and font scale overrides.
