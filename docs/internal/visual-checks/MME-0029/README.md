# MME-0029 Visual Checks

Artifacts captured by `npm run visual:mme-0029` against the local demo URL.

- `block-handle-hover-focus.png` proves top-level rich blocks expose focusable block handles.
- `block-menu-keyboard.png` proves the block menu opens from the keyboard-reachable handle.
- `block-reordered-targeted.png` proves state-level reorder is reflected in the Markdown source.
- `selection-bubble-toolbar-ai.png` proves selected rich text opens the contextual toolbar with AI.
- `empty-placeholder.png` proves the empty rich document placeholder uses the localized slash hint.
