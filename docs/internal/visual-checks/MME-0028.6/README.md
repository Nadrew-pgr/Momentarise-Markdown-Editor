# MME-0028.6 Visual Checks

Generated by:

```sh
MME_DEMO_URL=http://127.0.0.1:5174/ npm run visual:mme-0028.6
```

Artifacts:

- `ai-provider-default-mock.png` — proves the default demo remains mock/offline unless a host provider adapter is configured.
- `ai-provider-host-managed.png` — proves host-managed provider state and endpoint/model disclosure are visible without raw browser key entry.
- `ai-provider-personal-byok-staged.png` — proves personal BYOK can stage a provider-backed suggestion while keeping the browser key out of visual/test state.
- `ai-provider-policy-blocked.png` — proves Document Access Policy blocks provider egress before a host-managed transport call.
