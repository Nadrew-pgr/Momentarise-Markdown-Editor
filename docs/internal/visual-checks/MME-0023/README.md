# MME-0023 visual checks

## `ai-assistant-popover.png`

Captured after opening an editor-native AI command without a session. It proves the AI assistant renders as a compact fixed popover, preserves explicit memory-only private-session setup, and does not push the Markdown editor surface down.
