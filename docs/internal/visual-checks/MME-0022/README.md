# MME-0022 Visual Check

Artifacts:

- `source-keymap-integrity.png` — captured after the browser-driven source-mode keymap scenario. The script verifies bullet, checkbox, and blockquote continuation; empty bullet exit; smart Backspace after a marker; and `Mod-s` writing through the demo save hook.

The behavioral assertions in `scripts/visual-check-mme0022.mjs` are the primary proof. The screenshot records the browser/editor surface used for that proof.
