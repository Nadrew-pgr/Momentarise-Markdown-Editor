# AI Review Reports

Generated Claude framework review reports are written in this folder.

Reports are ignored by Git by default because they may include large excerpts of local diffs and working-state analysis.

Keep durable decisions in `docs/internal/build-log.md`, `docs/internal/ISSUES.md`, or `MME-BACKLOG` instead of relying on generated reports as canonical project state.
